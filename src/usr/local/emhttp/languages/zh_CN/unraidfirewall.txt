Unraid Firewall=Unraid 防火墙
Safety warning=安全警告：
apply rules only after confirming that your management source IP is allowed=请先确认管理端来源 IP 已被允许，再应用规则。
The plugin manages host=此插件管理主机
traffic and Docker bridge forwarding via=流量以及 Docker 网桥转发（通过
it does not replace Unraid or Dockers existing firewall rules=它不会替换 Unraid 或 Docker 现有的防火墙规则。
Firewall policy=防火墙策略
Enable firewall policy=启用防火墙策略
Apply the IPv4IPv6 policies below=应用下面的 IPv4/IPv6 策略
When disabled, the plugin removes only its own chains and leaves existing Unraid rules unchanged=禁用后，插件只会删除自己的链，不会修改现有的 Unraid 防火墙规则。
IPv4=IPv4
iptables=iptables
Enable IPv4 rules=启用 IPv4 规则
Default allow inbound=默认允许入站
Allow sources that do not match a rule=允许与规则不匹配的来源
IPv4 inbound rules=IPv4 入站规则
Name=名称
Action=动作
Source IPCIDR=来源 IP/CIDR
Protocol=协议
Destination port=目标端口
Web UI=Web UI
Allow=允许
Deny=拒绝
19216810 24 or blank=192.168.1.0/24 或留空
Any=任意
TCP=TCP
UDP=UDP
5000 or 5000-5010=5000 或 5000-5010
Remove rule=删除规则
Remove=删除
Add IPv4 rule=添加 IPv4 规则
Rules are evaluated top-to-bottom Leave source blank for any source Leave port blank for all ports A port requires TCP or UDP=规则将按从上到下的顺序匹配。来源留空表示任意来源。端口留空表示所有端口。指定端口时必须选择 TCP 或 UDP。
IPv6=IPv6
ip6tables=ip6tables
Enable IPv6 rules=启用 IPv6 规则
IPv6 inbound rules=IPv6 入站规则
Use IPv6 addresses or CIDRs Leave source blank for any source Leave port blank for all ports A port requires TCP or UDP=使用 IPv6 地址或 CIDR。来源留空表示任意来源。端口留空表示所有端口。指定端口时必须选择 TCP 或 UDP。
Add IPv6 rule=添加 IPv6 规则
Apply settings=应用设置
Last apply state=上次应用状态：
Active=已启用
Inactive=未启用
Error applying rules=应用规则时出错
Unknown=未知
Applying firewall rules=正在应用防火墙规则……
The firewall rules could not be applied=无法应用防火墙规则。
The WebUI could not contact the firewall service=WebUI 无法连接防火墙服务。
The firewall request timed out=防火墙请求超时。
Firewall rules applied=防火墙规则已应用。
Firewall rules saved and applied=防火墙规则已保存并应用。
Firewall settings saved Applying rules=防火墙设置已保存，正在应用规则……
The firewall settings could not be saved=无法保存防火墙设置。
